# Dependencies
node_modules/

# Build output: tsc(host) -> lib/*.js, tsc(client) + bundle -> lib/client.js.
# Not tracked; rebuilt by the `prepare` script on install and before publish.
lib/

# Logs
*.log
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Local-only planning notes. Not part of the plugin and not meant to be
# distributed: they carry deployment-specific facts and personal paths.
docs/atlassian-dsh-plugin-plan.md
